import React from "react";

import { StyledFooter } from "./styled";

import { Logo } from '/src/components/ui/logo/logo';

const Footer = () => {
  return (
    <StyledFooter>
      <Logo />
    </StyledFooter>
  );
};

export { Footer };
